#include <iostream>

using namespace std;

int sum(int first, int second)
{
    int result;
    result = first + second;
    return (result);
}

int subCall(int second, int third)
{
    int result;
    result = second + third;
    return (result);
}